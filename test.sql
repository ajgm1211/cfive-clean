SELECT ac.name,ac.date, IFNULL(ac.request_id, 'manual') as request_id  , IFNULL(ct.status, 'Contract erased') ,comp.name FROM ebdb.accounts_import_cfcl ac inner join contracts ct on ac.id = ct.account_id inner join company_users comp on ac.company_user_id = comp.id ;